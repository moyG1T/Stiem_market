<Page x:Class="Stiem_market.Pages.Store.StorePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Stiem_market.Pages.Store"
      mc:Ignorable="d" 
       d:DesignHeight="720" d:DesignWidth="1280"
  Title="StorePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <ListBox x:Name="GameBox" ItemsSource="{Binding Games}" Margin="0 40 0 0"
         ItemContainerStyle="{StaticResource GameList}" Grid.Row="0"
         VerticalAlignment="Top" HorizontalAlignment="Stretch"
         ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Disabled"
         Background="Transparent" BorderThickness="0" MouseDoubleClick="ListBox_MouseDoubleClick"/>
    </Grid>
</Page>
