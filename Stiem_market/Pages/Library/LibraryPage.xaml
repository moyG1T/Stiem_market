<Page x:Class="Stiem_market.Pages.Library.LibraryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Stiem_market.Pages.Library"
      mc:Ignorable="d" 
       d:DesignHeight="620" d:DesignWidth="1280"
  Title="LibraryPage">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition MinWidth="300" Width="1*"/>
            <ColumnDefinition Width="10"/>
            <ColumnDefinition MinWidth="660" Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="1" HorizontalAlignment="Stretch" MinWidth="300">
            <ListBox x:Name="GameListBox" ItemsSource="{Binding LibraryCollection}" 
             HorizontalAlignment="Stretch"
             Background="Transparent" BorderThickness="0"  MinWidth="300">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource TotallyBlack}" Width="Auto" Height="40" Padding="10 3 10 3" Margin="0 5 0 0" CornerRadius="15">
                            <TextBlock Text="{Binding Games.Title}" Style="{StaticResource MediumText}" HorizontalAlignment="Left"></TextBlock>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <Rectangle Fill="{StaticResource TotallyBlack}" Width="2" Height="Auto" HorizontalAlignment="Right" VerticalAlignment="Stretch" Grid.Column="2"></Rectangle>
        <StackPanel MinWidth="660"></StackPanel>
    </Grid>
</Page>
